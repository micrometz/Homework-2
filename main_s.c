
#include "matrix_static.h"

int main() 
{
 printf("Please enter matrix");
scanf("%d",T data[]);
  matrix  a;
  a = create_initvals(2,2,data);
  equate(&a);//I see this in the test not why why is there but add it just in case
  transpose(a);
 printf("\n Matrix a:");
}